import { config } from "@fortawesome/fontawesome-svg-core";
import "@fortawesome/fontawesome-svg-core/styles.css"; // Importation des styles
config.autoAddCss = false; // Désactive l'ajout automatique des styles CSS